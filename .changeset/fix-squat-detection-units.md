---
'good-morning': patch
---

スクワット検出が回数を増やせないバグを修正。expo-sensors の Accelerometer は g 単位（静止時 |a|≈1.0）で値を返すが、旧実装は m/s² 前提で 9.0/10.5 という閾値を使っており、g 単位では絶対に到達しないためカウントが一切増えなかった。閾値を g 単位に直し、低域通過フィルタ・最小フェーズ滞在時間・タイムアウトを追加してより堅牢な状態機械に書き直した。動作確認用に `stepDetector` のユニットテストも追加。
