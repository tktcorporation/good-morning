---
---

ci/test 基盤の強化。Metro バンドル検証 (`pnpm bundle:check`) を CI に追加し、画面ファイルの import スモークテスト・レンダースモークテストを導入することで、起動直後にクラッシュする原因（未解決 import、循環参照、トップレベル import 副作用での例外、初回レンダー時の throw）を CI で検知できるようにした。ユーザー向けの動作変更なし。
