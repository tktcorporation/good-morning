store-dir=.pnpm-store

# modules/expo-sky-vision は link: プロトコルで参照しているため、リンク先自身の
# dependencies（expo-modules-core）は pnpm install の対象にならない。ルートの
# node_modules に expo-modules-core を hoist させ、クリーン環境でも解決できるようにする。
public-hoist-pattern[]=expo-modules-core
