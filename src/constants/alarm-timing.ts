/**
 * 起床後ナッジ（スヌーズアラーム・TODO リマインド通知）の発火間隔・本数の
 * 単一定義（SSOT）。
 *
 * スヌーズアラームと TODO リマインド通知は「アラーム後 3 時間、9 分間隔で
 * 促す」という同じケイデンスを共有する。両者がそれぞれ別リテラルを持つと
 * 片方だけズレて挙動が食い違うため、ここに集約して同一値を参照させる。
 *
 * native ブリッジ（AlarmKitService）に依存しない純粋な定数モジュールに保つ
 * ことで、通知サービスからも循環依存なく参照できる。
 */

/** 起床後ナッジの間隔（秒）。iOS 標準アラームと同じ 9 分間隔。 */
export const SNOOZE_DURATION_SECONDS = 540;

/** 先行スケジュールするナッジの最大本数。9 分 × 20 = 3 時間分。 */
export const SNOOZE_MAX_COUNT = 20;
